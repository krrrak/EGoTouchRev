好的，让我先找到 TSAPrmt.dll 的 Ghidra 实例，然后直接从 `0x6861b180` 读取参数值。

Used MCP tool: ghydra/instances_list
Used MCP tool: ghydra/instances_use

Now let me read the parameter table. The table is 0xE40 bytes. Let me read all the critical offsets:

Used MCP tool: ghydra/memory_read
Used MCP tool: ghydra/memory_read

The first 8 bytes contain a pointer `0x6861A340` — this is a pointer-based reference, not inline data. Let me check — the variable might be a **pointer** to the table, not the table itself:

Used MCP tool: ghydra/memory_read
Used MCP tool: ghydra/data_list

It's a pointer! The actual table is at `0x6861A340`. Let me now read all the parameter values from the actual table:

Used MCP tool: ghydra/memory_read
Used MCP tool: ghydra/memory_read

OK, the first byte `0x01` = 1 HW parameter unit. The unit starts at offset +0x08. Let me now read the critical blocks:

Used MCP tool: ghydra/memory_read

Excellent! Non-zero data! Let me calculate: the algo params block starts at `+0x10` from unit start. Unit start = `0x6861A348`. So `+0x10` = `0x6861A358` → `DAT_1820d630`. But first the data starting at offset `+0x08` from table base (unit[0] header):

Base = `0x6861A340`
- `+0x00` = `0x01` (1 unit)
- `+0x08` = unit[0] starts at `0x6861A348`
  - `+0x00` relative to unit = HW version slots: `05 00 00` → hwVer=5
  - `+0x04` = `0x00FA` = 250, `0x00C8` = 200 → `DAT_1820d628`=0xFA=250, `DAT_1820d62a`=0xC8=200
  - `+0x08` → memcpy to DAT_1820d630, size 0x238

The memcpy source = `0x6861A348 + 0x08` = `0x6861A350`. `DAT_1820d630` = byte at offset 0 of this block = `0x0E` = 14 = `0b00001110`.

Let me read more of the non-zero data areas:

Used MCP tool: ghydra/memory_read
Used MCP tool: ghydra/memory_read

Now the critical area — let me read the triangle edge params at offset +0x218 and the global params at +0xA28:

Used MCP tool: ghydra/memory_read
Used MCP tool: ghydra/memory_read

Now the global params at offset `+0xA28` from table base `0x6861A340`:

Used MCP tool: ghydra/memory_read

Now I see the data is not exactly at `+0xA28`. Let me recalculate — the table base is `0x6861A340`, so `+0xA28` = `0x6861A340 + 0xA28` = `0x6861AD68`. Let me verify:

Used MCP tool: ghydra/memory_read
Used MCP tool: ghydra/memory_read

`0x3C` and `0x28` at offset `+0xA28` don't look like grid dimension bytes (should be 9-ish for a 9x9 grid). The flash table might be a different variant / version. Let me look more carefully. The `ASA_LoadProjectPrmt` says: `DAT_1820d610 = *(byte*)(param_2 + 0xA28)`. But `param_2` is `g_tsaPrmtFlashAsa` which is loaded on the **ASA side**, not the entire prmt table.

Wait — from `TSAPrmt_FlashPrmtLoadProject`:
```
local_28 = &g_tsaPrmtFlashAsa;
local_20 = 0xe40;
```

So the pointer `g_tsaPrmtFlashAsaGaokunHimaxCSOT` (at `6861b180`) points to a 0xE40-byte region used as the ASA parameters. Let me re-verify:

Used MCP tool: ghydra/memory_read

`0x01` = 1 HW unit. The table structure is:
- `+0x00`: unit count = 1
- `+0x08`: unit[0] starts (0x288 bytes per unit)
- `+0x08 + 0x288` = `+0x290`: unit[1] if exists
...
- After all units: global params at `+0xA28`

But with only 1 unit, the actual parameter block still has fixed offsets from the table base because the offsets are hardcoded in the code. Let me verify by reading at `+0xA28`:

`0x6861A340 + 0xA28 = 0x6861AD68`:
```
3C 28 50 0A
```

`0x3C` = 60, `0x28` = 40. These don't look like 9. Let me check — maybe the grid dimension field is loaded differently. Let me look at the actual data layout more carefully. Perhaps this table is compacted differently. Let me read the whole area around the unit more carefully:

Used MCP tool: ghydra/memory_read
Used MCP tool: ghydra/memory_read

From `ASA_LoadStylusHWPrmt`, the data layout within `g_asaPrmtFlash + unit*0x288 + offset`:
- unit=0, base = `g_asaPrmtFlash + 0*0x288 + 8` = `g_asaPrmtFlash + 0x8`
  
`g_asaPrmtFlash` = `0x6861A340`  
Unit[0] at `0x6861A348`:
- `+0x00`: `0x05` (hwVersion slot 0) 
- `+0x04`: `0x00FA`=250 → `DAT_1820d628`
- `+0x06`: `0x00C8`=200 → `DAT_1820d62a`
- `+0x08`: `0x0E`=14 → `DAT_1820d630` (算法标志)

Then memcpy from `g_asaPrmtFlash + unit*0x288 + 0x10` = `0x6861A340 + 0 + 0x10` = `0x6861A350`.

Let me now decode `DAT_1820d630 = 0x0E = 0b00001110`:

```
bit0 = 0 → 使用三角插值（不是重心法！）
bit1 = 1 → (未知)
bit2 = 1 → TX2 坐标使能
bit3 = 1 → 边缘补偿附加逻辑使能
bit6 = 0 → TP图案补偿禁用
```

Now let me read the critical polynomial coefficient areas. DAT_1820d638 corresponds to offset `+0x08` from the memcpy start (`0x6861A350`), so `0x6861A358`:

Used MCP tool: ghydra/memory_read
Used MCP tool: ghydra/memory_read

Line mode coefficients are all zero! Now let me check the Grid mode coefficients. DAT_1820d6C8 = `0x1820d630 + 0x98`, so memcpy offset `+0x98` = `0x6861A350 + 0x98` = `0x6861A3E8`:

Used MCP tool: ghydra/memory_read

Found non-zero coefficients! Let me decode these double values. The Grid dim1 coefficients at `0x6861A3F0`:

Used MCP tool: ghydra/memory_read
Used MCP tool: ghydra/memory_read

Now let me decode the hex to doubles using python. Let me also read the other Grid dim2 coefficients:

Ran command: `python3 -c "import struct; data1 = bytes.fromhex('0000000000000000F487379000005FE85FFB5FBF1D680546697800003F78CAB2987AA37200D5BE'); print('wrong, let me parse individual doubles'); d = [bytes.fromhex(h) for h in ['0000000000000000','F4873790E85FFBBF','5F1D68054669783F','CAB2987AA372D5BE','00000000000000F03F0000000000000000','0000000000000000','0000000000000000']]; print('Parse manually')"`
Ran command: `python3 -c "
import struct

# Grid dim1 coefficients (DAT_1820d6C8): c0, c1, c2, c3
# From memory at 0x6861A3E8 (little-endian doubles)
hexvals = [
    '0000000000000000',  # c0
    'F4873790E85FFBBF',  # c1
    '5F1D68054669783F',  # c2
    'CAB2987AA372D5BE',  # c3
]
print('=== Grid Dim1 CoorMultiOrderFitCompensate (DAT_1820d6C8) ===')
for i, h in enumerate(hexvals):
    val = struct.unpack('<d', bytes.fromhex(h))[0]
    print(f'  c[{i}] = {val:.15e}   (0x{h})')

# Grid dim2: next 4 doubles at 0x6861A408
hexvals2 = [
    '000000000000F03F',  # c0
    '0000000000000000',  # c1
    '0000000000000000',  # c2
    '0000000000000000',  # c3
]
print()
print('=== Grid Dim2 CoorMultiOrderFitCompensate (DAT_1820d708) ===')
for i, h in enumerate(hexvals2):
    val = struct.unpack('<d', bytes.fromhex(h))[0]
    print(f'  c[{i}] = {val:.15e}   (0x{h})')

# Now also decode the 2nd area with non-zero data at 0x6861A430
hexvals3 = [
    '177331177331F7BF',
    '23BC844141707C3F',  # Wait let me fix - let me recheck
    '5D0AB0D71E5ECFBE',
    '000000000000F03F',
]
print()
print('=== Area at offset +0xE0 from memcpy start ===')
for i, h in enumerate(hexvals3):
    val = struct.unpack('<d', bytes.fromhex(h))[0]
    print(f'  [{i}] = {val:.15e}   (0x{h})')
"
`

Let me now refine — the second non-zero block I saw earlier. Let me re-read it precisely:

Used MCP tool: ghydra/memory_read

Offset `0x6861A430` - memcpy start `0x6861A350` = `0xE0`. Let me check what `DAT_1820d630 + 0xE0` = `DAT_1820d710`. But wait — `DAT_1820d708` should be at offset `+0xD8` from memcpy. Let me recalculate:

`DAT_1820d6C8 - DAT_1820d630 = 0x98` → memcpy offset `+0x98` → address `0x6861A350 + 0x98 = 0x6861A3E8` ✓

`DAT_1820d708 - DAT_1820d630 = 0xD8` → memcpy offset `+0xD8` → address `0x6861A350 + 0xD8 = 0x6861A428`

Used MCP tool: ghydra/memory_read

Now let me also recalculate dim1. `DAT_1820d6C8 = 0x1820d6C8`, offset from `DAT_1820d630 = 0x98`. Memory = `0x6861A3E8`.

But the actual dim1 coefficients were at `0x6861A3E8`: first double is `0000000000000000` (c0=0), then `F4873790E85FFBBF` (c1=-1.71), `5F1D68054669783F` (c2=0.00596), `CAB2987AA372D5BE` (c3=-5.11e-6). But then the next 4 doubles at `0x6861A408` start with `000000000000F03F` (=1.0). That's at offset `+0xB8` → `DAT_1820d6E8`.

Let me re-verify the dim2 coefficients:

Ran command: `python3 -c "
import struct

# Grid Dim2 coefficients (DAT_1820d708) at 0x6861A428
# c0, c1, c2, c3
hexvals = [
    '0000000000000000',  # c0  (at 0x6861A428)
    '177331177331F7BF',  # c1  (at 0x6861A430)
    '23BC844141707C3F',  # c2  (at 0x6861A438) - wait I need to recheck
    '5D0AB0D71E5ECFBE',  # c3  (at 0x6861A440)
]
print('=== Grid Dim2 CoorMultiOrderFitCompensate (DAT_1820d708) ===')
for i, h in enumerate(hexvals):
    val = struct.unpack('<d', bytes.fromhex(h))[0]
    print(f'  c[{i}] = {val:.15e}')

# Re-check: what is at 0x6861A438?
"
`
Ran command: `python -c "import struct; hexvals = ['0000000000000000','F4873790E85FFBBF','5F1D68054669783F','CAB2987AA372D5BE']; print('=== Grid Dim1 (DAT_1820d6C8) ==='); [print(f'  c[{i}] = {struct.unpack(chr(60)+chr(100), bytes.fromhex(h))[0]:.15e}') for i,h in enumerate(hexvals)]; hexvals2 = ['0000000000000000','177331177331F7BF','23BC844141707C3F','5D0AB0D71E5ECFBE']; print('=== Grid Dim2 (DAT_1820d708) ==='); [print(f'  c[{i}] = {struct.unpack(chr(60)+chr(100), bytes.fromhex(h))[0]:.15e}') for i,h in enumerate(hexvals2)]"`

Wait, I need to re-verify dim2 — the `c2` hex was `23BC844141707C3F` at my earlier read, let me re-verify:

Used MCP tool: ghydra/memory_read
Ran command: `python -c "import struct; v=struct.unpack('<d',bytes.fromhex('23BC844141707C3F'))[0]; print(f'c2 = {v:.15e}')"`

Good. But wait — in my earlier read at `0x6861A408` (what I initially labeled as `DAT_1820d708`), the first double was `000000000000F03F` = 1.0. That was wrong — that's actually at offset `+0xB8` from memcpy, not `+0xD8`. Let me check what `+0xB8` maps to:

`DAT_1820d630 + 0xB8 = DAT_1820d6E8` — this is between the two coefficient groups. Let me now map this correctly and check the value at `+0xB8`:

Used MCP tool: ghydra/memory_read

The `1.0` at `+0xB8` is likely part of another parameter (perhaps a scaling factor). Now let me read the edge interpolation parameters and the global tail parameters:

Used MCP tool: ghydra/memory_read
Used MCP tool: ghydra/memory_read

Now let me read the global parameters at the correct offsets. First the grid dimensions at `+0xA28`:

Used MCP tool: ghydra/memory_read
Used MCP tool: ghydra/memory_read
Used MCP tool: ghydra/memory_read

`0x3C` = 60, `0x28` = 40 — these are NOT grid dimensions (should be ~9). The table may have a different alignment. Let me verify by checking the entire data section. Perhaps the table isn't a flat byte array but has pointer indirection. Let me check:

Used MCP tool: ghydra/memory_read
Used MCP tool: ghydra/data_list

Let me check the size of this table. There should be `0xE40` bytes. But maybe the offsets in TSACore's `ASA_LoadProjectPrmt` don't match what's actually stored. Let me take a different approach — look for reasonable grid dimension values (9 for a 9x9 grid) by scanning:

Ran command: `python -c "
# Scan the table to find bytes with value 0x09 near offset 0xA28
# Table base = 0x6861A340, size = 0xE40
# Let me search around offset 0xA28 for value 9
# But first let me try to understand: maybe the pointer points to a struct that wraps the data
# The var is 'float *' type, maybe it's an array of floats?
# Or maybe the actual layout in memory is different from what we assume

# Let's try: The key is that ASA_LoadProjectPrmt receives its 2nd arg as g_asaPrmtFlashAsa
# TSAPrmt_FlashPrmtLoadProject does:
#   local_28 = &g_tsaPrmtFlashAsa    (address of the buffer)
#   local_20 = 0xe40                  (size)
# This buffer gets filled by the callback
# Then ASA_LoadProjectPrmt(tsaParams, g_tsaPrmtFlashAsa) is called
# where g_tsaPrmtFlashAsa is the VALUE of the buffer (not pointer to pointer)

# Since g_tsaPrmtFlashAsaGaokunHimaxCSOT is declared as 'float*' pointing to 0x6861A340
# It could be that 0x6861A340 IS the buffer content (the table data itself)
# The callback copies FROM this static table INTO the runtime g_tsaPrmtFlashAsa buffer

# So 0x6861A340 is the actual parameter table. Unit count = 1 at byte 0.
# The issue is the per-unit structure in the flash table may have 4-byte alignment
# vs the code accessing it with byte offsets

# Actually wait - look at ASA_LoadProjectPrmt:
# g_asaPrmtFlash = param_2;    ← this is the WHOLE buffer
# DAT_1820d610 = *(byte*)(param_2 + 0xa28);
# So param_2 + 0xa28 => 0x6861A340 + 0xA28 = 0x6861AD68
# The value there was 0x3C = 60

print('Grid dims at +0xA28: gridCols=0x3C=60, gridRows=0x28=40')
print('These are the FULL PANEL dimensions, not the 9x9 grid!')
print('60 columns and 40 rows is a typical large touch panel')
"
`

60×40 makes perfect sense for a Gaokun panel! Now let me read all the IIR and jitter parameters:

Used MCP tool: ghydra/memory_read
Used MCP tool: ghydra/memory_read

Now let me compute the correct offsets for the IIR and jitter params: 
- `+0xA58` from base = `0x6861A340 + 0xA58 = 0x6861AD98`
- `+0xA68` from base = `0x6861A340 + 0xA68 = 0x6861ADA8`
- `+0xA84` from base = `0x6861A340 + 0xA84 = 0x6861ADC4`

Used MCP tool: ghydra/memory_read

Now let me put it all together with proper decoding:

Ran command: `python -c "
import struct

# ======== Edge params: DAT_1820d848 at memcpy_offset +0x218 ========
# memcpy start = 0x6861A350  → +0x218 = 0x6861A568

# dim1 edge params: int16[3] at 0x6861A568
dim1_edge_hex = '320088138813'
d1 = [struct.unpack_from('<h', bytes.fromhex(dim1_edge_hex), i*2)[0] for i in range(3)]
print(f'Dim1 Edge params: param1={d1[0]}, sumThreshold_L={d1[1]}, sumThreshold_R={d1[2]}')

# dim2 edge params: int16[3] at 0x6861A56E
dim2_edge_hex = '320094117411'  # wait let me re-verify from the hex dump
# From dump at 0x6861A568: 32 00 88 13 88 13 32 00 94 11 74 0E
#                          ───dim1[0]─── ───dim1[1]─── ───dim1[2]───
# Wait, these are int16 little-endian:
# 32 00 = 0x0032 = 50
# 88 13 = 0x1388 = 5000 
# 88 13 = 0x1388 = 5000
# 32 00 = 0x0032 = 50
# 94 11 = 0x1194 = 4500
# 74 0E = 0x0E74 = 3700
vals = bytes.fromhex('3200881388133200941174E')
# Let me be more careful
raw = bytes.fromhex('320088138813320094117A0E')
print()
print('=== Raw int16 values starting at 0x6861A568 ===')
for i in range(6):
    v = struct.unpack_from('<h', raw, i*2)[0]
    print(f'  [{i}] = {v} (0x{v:04X})')
"
`
Ran command: `python -c "import struct; raw=bytes.fromhex('32008813881332009411740E'); vals=[struct.unpack_from('<h',raw,i*2)[0] for i in range(6)]; print('Edge params int16:'); [print(f'  [{i}]={v}') for i,v in enumerate(vals)]"`

Now let me decode the IIR and jitter parameters:

Ran command: `python -c "
import struct

# ===== Full parameter decode =====
# All offsets relative to table base 0x6861A340

# +0xA28 = 0x6861AD68: gridCols, gridRows
print('=== Grid Dimensions (+0xA28) ===')
print(f'  gridCols (DAT_1820d610) = 0x3C = 60')
print(f'  gridRows (DAT_1820d611) = 0x28 = 40')

# +0xA58 = 0x6861AD98: jitter thresholds
# Raw: 01 02 00 00 06 12 02 10
print()
print('=== Jitter Thresholds (+0xA58) ===')
raw_jitter = bytes.fromhex('0102000006120210')
print(f'  edgeJitterX  (+0xA58) = {raw_jitter[0]}')
print(f'  edgeJitterY  (+0xA59) = {raw_jitter[1]}')
print(f'  centerJitterX (+0xA5A) = {raw_jitter[2]}')
print(f'  centerJitterY (+0xA5B) = {raw_jitter[3]}')

# Wait... these values look odd. Let me re-read
# +0xA58 from base: 0x6861AD98
# Raw bytes: 01 02 00 00 06 12 02 10
# But these are supposed to be single bytes

# Actually let me reconsider: 01=1, 02=2, 00=0, 00=0
# That seems low for jitter. Let me continue
print(f'  writeIIR_low  (+0xA5C) = {raw_jitter[4]}')
print(f'  writeIIR_high (+0xA5D) = {raw_jitter[5]}')
print(f'  hoverIIR_low  (+0xA5E) = {raw_jitter[6]}')
print(f'  hoverIIR_high (+0xA5F) = {raw_jitter[7]}')

# +0xA60 = 0x6861ADA0: IIR denominator N
raw2 = bytes.fromhex('2000200084031E0E')
print()
print('=== IIR Denominator & Scale (+0xA60) ===')
print(f'  IIR_N (+0xA60) = {raw2[0]}')

# +0xA68 = 0x6861ADA8: totalPitch
raw3 = bytes.fromhex('000A4006')
pitch1 = struct.unpack_from('<H', raw3, 0)[0]
pitch2 = struct.unpack_from('<H', raw3, 2)[0]
print()
print('=== Pitch Dimensions (+0xA68) ===')
print(f'  totalPitchDim1 (g_asaPrmt) = {pitch1} = 0x{pitch1:X}')
print(f'  totalPitchDim2 (DAT_1820d602) = {pitch2} = 0x{pitch2:X}')

# +0xA84 = 0x6861ADC4: signal refresh mode
print()
print('=== Signal Refresh Mode (+0xA84) ===')
print(f'  refreshMode = 0 (standard, not PR)')
"
`

Now let me re-read the byte at `+0xA60` more carefully:

Used MCP tool: ghydra/memory_read

`0x20` = 32 — so IIR_N = 32 (not 256!). And `+0xA68`:

Used MCP tool: ghydra/memory_read

Now I have everything. Let me compile the complete parameter table output.

---

好的，以下是从 `TSAPrmt.dll` 中 `g_tsaPrmtFlashAsaGaokunHimaxCSOT` (指针 `@6861b180` → 数据 `@6861A340`) 提取到的全部参数值：

## 完整参数表提取结果

### 表头 (`+0x00`)

| 偏移 | 地址 | 值 | 含义 |
|------|------|-----|------|
| `+0x00` | `6861A340` | `0x01` = **1** | HW 参数单元数量（仅 1 套参数） |

### 参数单元 #0 (`+0x08`, stride=0x288)

| 偏移 | 地址 | 值 | 运行时变量 | 含义 |
|------|------|-----|-----------|------|
| `+0x08` | `6861A348` | `0x05` = **5** | hwVersion slot[0] | 匹配 HW 版本 5 |
| `+0x0C` | `6861A34C` | `0x00FA` = **250** | `DAT_1820d628` | 未知参数 |
| `+0x0E` | `6861A34E` | `0x00C8` = **200** | `DAT_1820d62a` | 未知参数 |

### 核心算法参数块（memcpy `+0x10` → `DAT_1820d630`, 0x238 bytes）

**算法选择标志 `DAT_1820d630` = `0x0E` = `0b00001110`**

```
bit0 = 0 → ★ 使用三角插值（非重心法）
bit1 = 1 → 附加功能使能
bit2 = 1 → TX2 坐标计算使能（用于倾斜角）
bit3 = 1 → 边缘补偿附加逻辑使能
bit6 = 0 → TP 图案补偿禁用
```

### ★ CoorMultiOrderFitCompensate 多项式系数

**Grid 模式 Dim1 (DAT_1820d6C8) — 偏移 `+0x98` from memcpy**

| 系数 | 地址 | 十六进制 | 值 |
|------|------|---------|-----|
| c[0] | `6861A3E8` | `0000000000000000` | **0.0** |
| c[1] | `6861A3F0` | `F4873790E85FFBBF` | **-1.710915149** |
| c[2] | `6861A3F8` | `5F1D68054669783F` | **+0.005959772** |
| c[3] | `6861A400` | `CAB2987AA372D5BE` | **-5.113556e-6** |

**Grid 模式 Dim2 (DAT_1820d708) — 偏移 `+0xD8` from memcpy**

| 系数 | 地址 | 十六进制 | 值 |
|------|------|---------|-----|
| c[0] | `6861A428` | `0000000000000000` | **0.0** |
| c[1] | `6861A430` | `177331177331F7BF` | **-1.449572650** |
| c[2] | `6861A438` | `23BC844141707C3F` | **+0.006942992** |
| c[3] | `6861A440` | `5D0AB0D71E5ECFBE` | **-3.739316e-6** |

**Line 模式 Dim1 (DAT_1820d638) — 偏移 `+0x08`**: **全零**（未启用）

**Line 模式 Dim2 (DAT_1820d658) — 偏移 `+0x28`**: **全零**（未启用）

### 三角插值边缘参数 (DAT_1820d848 / DAT_1820d84E)

**Dim1 边缘参数** — 偏移 `+0x218`

| 索引 | 地址 | 值 | 含义 |
|------|------|-----|------|
| [0] param1 | `6861A568` | **50** | 边缘补偿比例因子 |
| [1] sumThreshold_L | `6861A56A` | **5000** | 左边缘信号总量最低阈值 |
| [2] sumThreshold_R | `6861A56C` | **5000** | 右边缘信号总量最低阈值 |

**Dim2 边缘参数** — 偏移 `+0x21E`

| 索引 | 地址 | 值 | 含义 |
|------|------|-----|------|
| [0] param1 | `6861A56E` | **50** | 边缘补偿比例因子 |
| [1] sumThreshold_L | `6861A570` | **4500** | 左边缘信号总量最低阈值 |
| [2] sumThreshold_R | `6861A572` | **3700** | 右边缘信号总量最低阈值 |

### 全局参数 (`+0xA28` 之后)

| Flash 偏移 | 地址 | 值 | 运行时变量 | 含义 |
|-----------|------|-----|-----------|------|
| `+0xA28` | `6861AD68` | **60** (0x3C) | `DAT_1820d610` | gridCols — 面板列数 |
| `+0xA29` | `6861AD69` | **40** (0x28) | `DAT_1820d611` | gridRows — 面板行数 |
| `+0xA58` | `6861AD98` | **1** | — | 边缘区域 X 抖动阈值 |
| `+0xA59` | `6861AD99` | **2** | — | 边缘区域 Y 抖动阈值 |
| `+0xA5A` | `6861AD9A` | **0** | — | 中心区域 X 抖动阈值 |
| `+0xA5B` | `6861AD9B` | **0** | — | 中心区域 Y 抖动阈值 |
| `+0xA5C` | `6861AD9C` | **6** | — | 书写态 IIR 低速系数 |
| `+0xA5D` | `6861AD9D` | **18** (0x12) | — | 书写态 IIR 高速系数 |
| `+0xA5E` | `6861AD9E` | **2** | — | 悬停态 IIR 低速系数 |
| `+0xA5F` | `6861AD9F` | **16** (0x10) | — | 悬停态 IIR 高速系数 |
| `+0xA60` | `6861ADA0` | **32** (0x20) | — | **IIR 分母 N**（不是 256！） |
| `+0xA68` | `6861ADA8` | **2560** (0x0A00) | `g_asaPrmt` | totalPitchDim1 |
| `+0xA6A` | `6861ADAA` | **1600** (0x0640) | `DAT_1820d602` | totalPitchDim2 |
| `+0xA84` | `6861ADC4` | **0** | — | 信号刷新模式 (0=标准) |

### 关键发现总结

1. **算法选择**：`bit0 = 0` → **Gaokun 默认使用三角插值**，不是重心法

2. **Grid 模式补偿系数非零**：
   - Dim1: `f(x) = -1.711·x + 0.00596·x² - 5.11e-6·x³`
   - Dim2: `f(x) = -1.450·x + 0.00694·x² - 3.74e-6·x³`
   - 这些系数确认了跨格点补偿的**确实需要启用**！

3. **IIR 分母 N = 32**（不是之前假设的 256）：
   - IIR 公式: `out = (coef × new + (32-coef) × old) / 32`
   - 书写态：低速 coef=6（强滤波 α≈19%），高速 coef=18（弱滤波 α≈56%）
   - 悬停态：低速 coef=2（极强 α≈6%），高速 coef=16（中等 α≈50%）

4. **面板尺寸**：60 列 × 40 行，总长 dim1=2560, dim2=1600

5. **TP 图案补偿**：`bit6 = 0` → **禁用**（你不需要实现）

6. **Line 模式补偿系数全零** → 只有 Grid 模式需要多项式补偿